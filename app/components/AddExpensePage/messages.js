/*
 * AddExpensePage Messages
 *
 * This contains all the text for the AddExpensePage component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.AddExpensePage";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the AddExpensePage component!"
  }
});
