/*
 * EditExpensePage Messages
 *
 * This contains all the text for the EditExpensePage component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.EditExpensePage";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the EditExpensePage component!"
  }
});
