/*
 * HelpPage Messages
 *
 * This contains all the text for the HelpPage component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.HelpPage";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the HelpPage component!"
  }
});
