/*
 * EditExpense Messages
 *
 * This contains all the text for the EditExpense container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.EditExpense";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the EditExpense container!"
  }
});
