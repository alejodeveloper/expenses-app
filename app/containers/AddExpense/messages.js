/*
 * AddExpense Messages
 *
 * This contains all the text for the AddExpense container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.AddExpense";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the AddExpense container!"
  }
});
